{
  "name": "jarvis",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\" && tsc -p tsconfig.json",
    "build:web": "npm --prefix web run build",
    "build:all": "npm run build && npm run build:web",
    "start": "node dist/index.js",
    "test": "npm run build && node --test dist/tests/config.test.js dist/tests/orchestrator.test.js dist/tests/http-server.test.js",
    "dev": "tsc-watch --onSuccess \"node dist/index.js\"",
    "dev:web": "npm --prefix web run dev",
    "dev:all": "concurrently \"npm run dev\" \"npm run dev:web\"",
    "setup": "npm run build && node dist/index.js --setup",
    "tool:new": "node scripts/scaffold-tool.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Jarvis",
  "type": "module",
  "devDependencies": {
    "concurrently": "^9.2.1",
    "ts-node": "^10.9.2"
  },
  "dependencies": {
    "@openrouter/sdk": "^0.12.11",
    "@types/express": "^5.0.6",
    "@types/node": "^25.6.0",
    "@types/pg": "^8.20.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "pg": "^8.20.0",
    "tsc-watch": "^7.2.0",
    "typescript": "^6.0.2",
    "zod": "^4.3.6"
  }
}
